# Portugal Active — robots.txt
# We welcome AI/LLM crawlers so our content can be cited in ChatGPT,
# Perplexity, Claude, Google AI Overviews, Bing Copilot and others.
# Private/auth-gated paths are disallowed for every bot.

# ── Default policy: all crawlers ─────────────────────────────────────
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /*/admin/
Disallow: /api/
Disallow: /booking/
Disallow: /*/booking/
Disallow: /search?
# NOTE: /login, /account and /*.php were disallowed here, and that is exactly
# how they became "Indexada, mas bloqueada pelo ficheiro robots.txt" — Google
# had them in the index, could not fetch them, and so never saw that their
# markup said "index, follow". Blocking cannot remove a page from the index;
# only a noindex it is allowed to read can. Those routes now serve noindex
# themselves (AUTH_NOINDEX_PREFIXES in server/_core/vite.ts) and /index.php
# 301s, so Google is let in to see it. Same reasoning as the legacy paths.
# NOTE: the old Webflow/WordPress paths (/properties/, /rooms/, /lodges/,
# /journal/, /category/, /tag/, /author/) were listed here to save crawl
# budget. That was the wrong trade. All of them return 301 to a real page —
# verified 2026-08-24 — but a blocked path is never fetched, so the redirect
# is never seen and the authority those URLs still hold never reaches the
# pages that replaced them. They stayed stuck in the index instead, which is
# why they kept showing up in search. Crawl budget is cheap; the link equity
# on a decade of old URLs is not. Leave them crawlable.
#
# /wp-admin/ and /wp-login.php stay blocked: they also 301, but to the
# homepage, so there is no equity to recover — and they are attack-surface
# noise, not content.
Disallow: /wp-admin/
Disallow: /wp-login.php

# ── AI & LLM crawlers (training + retrieval + on-demand fetch) ───────
# Grouped explicitly so they inherit the same disallow list as * above.
# Per robots.txt spec, a bot only follows the most-specific matching
# group — without this block, AI bots would be allowed on /admin, /api,
# /login, etc. because they'd skip the * group entirely.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: anthropic-ai
User-agent: Google-Extended
User-agent: GoogleOther
User-agent: CCBot
User-agent: FacebookBot
User-agent: Meta-ExternalAgent
User-agent: Applebot-Extended
User-agent: Bytespider
User-agent: Amazonbot
User-agent: Diffbot
Allow: /
Disallow: /api/
Disallow: /admin/
Disallow: /*/admin/
Disallow: /account
Disallow: /*/account
Disallow: /login
Disallow: /*/login
Disallow: /booking/confirmation/
Disallow: /*/booking/confirmation/

Sitemap: https://www.portugalactive.com/sitemap.xml
